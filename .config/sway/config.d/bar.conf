# If we don't give the bar id, i.e. bar-0, another bar is created by sway.
# See here: https://github.com/swaywm/sway/issues/3946
bar bar-0 {
    position top
    colors {
        focused_background #1a1b2651
    }
}
